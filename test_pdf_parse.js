
const fs = require('fs');
const pdf = require('pdf-parse');
async function run() {
  const dataBuffer = fs.readFileSync('/tmp/gse_GOIL_1716649280000.pdf'); // We don't know the exact file name... let's just download it again
}
